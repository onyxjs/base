{
  "name": "onyx",
  "version": "0.0.0",
  "description": "WIP: A modular modern TypeScript testing framework.",
  "repository": "git@github.com:onyxjs/onyx.git",
  "author": "Elijah Kotyluk <elijah@elijahkotyluk.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "commit": "npx git-cz",
    "test": "lerna run test --stream",
    "lint": "lerna run --no-bail lint --stream",
    "lint:fix": "lerna run --no-bail lint:fix --stream",
    "lint:all": "eslint .",
    "build": "lerna run build --stream",
    "dev": "lerna run dev --stream",
    "watch": "lerna run watch --stream"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint:all --fix --concurrency 1 --since HEAD"
    }
  },
  "lint-staged": {
    "*.[jt]s": [
      "lerna run lint:all --fix",
      "git add",
      "lerna run test -- --bail --findRelatedTests"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.15.0",
    "@typescript-eslint/parser": "^4.15.0",
    "commitizen": "4.2.1",
    "eslint": "^7.20.0",
    "eslint-config-prettier": "^7.2.0",
    "eslint-plugin-prettier": "^3.3.1",
    "husky": "^2.7.0",
    "lerna": "3.21.0",
    "lint-staged": "^8.2.1",
    "prettier": "^2.2.1"
  }
}
